{% extends 'base.html.twig' %}

{% block title %}Commune index
{% endblock %}

{% block body %}
	<h2>Liste communes</h2>
    {% for message in app.flashes('commune') %}
                <div class="primary">
                    {{ message }}
                </div>
            {% endfor %}
<div><a href="{{ path('app_commune_new') }}">Nouveau</a></div>
	<table class="table">
		<thead>
			<tr>

				<th>Libelle</th>
				<th>Modifier</th>
				<th>Supprimer</th>
			</tr>
		</thead>
		<tbody>
			{% for commune in communes %}
				<tr>

					<td>{{ commune.libelle }}</td>
					<td>
						
						<a href="{{ path('app_commune_edit', {'id': commune.id}) }}">Modifier</a>
					</td>
					<td>
						<form method="post" action="{{ path('app_commune_delete', {'id': commune.id}) }}" onsubmit="return confirm('Voulez-vous supprimer?');">
							<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commune.id) }}">

							<button class="btn">
								<span style="font-size: 1em; color: red;">
									<i class="fas fa-trash-alt"></i>
								</span>
                                supp
							</button>
						</form>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="3">Aucune donnée trouvée</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	
{% endblock %}
