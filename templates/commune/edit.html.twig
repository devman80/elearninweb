{% extends 'base.html.twig' %}

{% block title %}Edition Commune{% endblock %}

{% block body %}
    <h1>Edition Commune</h1>

    {{ include('commune/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_commune_index') }}">back to list</a>

    {{ include('commune/_delete_form.html.twig') }}
{% endblock %}
