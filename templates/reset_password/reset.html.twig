

{% block title %}Renitialiser{% endblock %}

{% block body %}
    <h1>Renitialiser votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Renitialiser</button>
    {{ form_end(resetForm) }}
{% endblock %}
