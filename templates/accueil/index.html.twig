{% extends 'base.html.twig' %}

{% block title %}Hello Accueil Controller!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<a class="collapse-item" href="{{ path('app_user_index') }}">Utilisateur</a>
	<div class="example-wrapper">

		<div>{{ render_chart(chart) }}</div>
	</div>
{% endblock %}
