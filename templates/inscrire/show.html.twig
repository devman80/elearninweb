{% extends 'base.html.twig' %}

{% block title %}Inscrire{% endblock %}

{% block body %}
    <h1>Inscrire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscrire.id }}</td>
            </tr>
            <tr>
                <th>Dateinscrire</th>
                <td>{{ inscrire.dateinscrire ? inscrire.dateinscrire|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscrire_index') }}">back to list</a>

    <a href="{{ path('app_inscrire_edit', {'id': inscrire.id}) }}">edit</a>

    {{ include('inscrire/_delete_form.html.twig') }}
{% endblock %}
