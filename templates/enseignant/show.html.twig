{% extends 'base.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enseignant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enseignant.prenom }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ enseignant.contact }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ enseignant.sexe }}</td>
            </tr>
            <tr>
                <th>Facebook</th>
                <td>{{ enseignant.facebook }}</td>
            </tr>
            <tr>
                <th>Twitter</th>
                <td>{{ enseignant.twitter }}</td>
            </tr>
            <tr>
                <th>Instagram</th>
                <td>{{ enseignant.instagram }}</td>
            </tr>
            <tr>
                <th>BrochureFilename</th>
                <td>{{ enseignant.brochureFilename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseignant_index') }}">back to list</a>

    <a href="{{ path('app_enseignant_edit', {'id': enseignant.id}) }}">edit</a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
