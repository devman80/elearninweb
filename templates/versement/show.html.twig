{% extends 'base.html.twig' %}

{% block title %}Versement{% endblock %}

{% block body %}
    <h1>Versement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ versement.id }}</td>
            </tr>
            <tr>
                <th>Montantversement</th>
                <td>{{ versement.montantversement }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ versement.code }}</td>
            </tr>
            <tr>
                <th>Dateversement</th>
                <td>{{ versement.dateversement ? versement.dateversement|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_versement_index') }}">back to list</a>

    <a href="{{ path('app_versement_edit', {'id': versement.id}) }}">edit</a>

    {{ include('versement/_delete_form.html.twig') }}
{% endblock %}
