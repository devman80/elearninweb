<form method="post" action="{{ path('app_versement_delete', {'id': versement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ versement.id) }}">
    <button class="btn">Delete</button>
</form>
